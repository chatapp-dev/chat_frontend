const ENDPOINTS = {
    auth: {
        register: '/users/register',
        login: '/users/login',
        current: '/users/current',
        logout: '/users/logout'
    }
}

export default ENDPOINTS;